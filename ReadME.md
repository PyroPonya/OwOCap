# OwOCap

#### — это десктопный ассистент нового поколения, который превращает любой текст или голос с вашего компьютера в интеллектуальный ответ. **_Capture this, uwu_**

#### Мы создали два продукта в одном с модульной архитектурой:

### 🎯 Базовая версия (V1) — Instant Text Grabber

**Три режима для мгновенного захвата информации одним нажатием клавиши:**

1. Скриншот → Текст: Выделил область экрана — получил распознанный текст

2. Микрофон → Текст: Нажал клавишу — говоришь, получаешь расшифровку

3. Системный звук → Текст: Перехватывает любой звук из приложений (видео, звонки, стримы) и расшифровывает его

### 🚀 Продвинутая версия (V2) — AI Assistant Pro

**Всё, что умеет V1, плюс интеграция с нейросетью:**

- Захваченный текст автоматически анализируется AI

- Получаете развернутые ответы, переводы, объяснения, резюме

- Умный контекст: нейросеть понимает, работаете вы с кодом, статьей или диалогом

- История запросов и ответов в удобном интерфейсе

### 🖥️ Унифицированный интерфейс для обеих версий:

- Единое окно с настройками горячих клавиш, качества распознавания и выбора языков

- Панель истории: слева — захваченный текст, справа (в V2) — ответы нейросети

- Минималистичный дизайн, работает поверх всех окон, не мешая работе

Установка:

#### Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Загрузка модели Vosk:

```bash
# Скачайте модель (пример для маленькой русской модели)
# Посетите https://alphacephei.com/vosk/models
# Скачайте vosk-model-small-ru-0.22.zip
# Разархивируйте в папку models
```

#### Запуск приложения:

```bash
python main.py
```

#### Сборка в исполняемый файл:

```bash
python build_exe.py
```

#### Основные функции:

1. Горячие клавиши по умолчанию:

   - Ctrl+Shift+A - захват области экрана

   - Ctrl+Shift+M - захват звука (микрофон)

   - Ctrl+Shift+S - захват звука (системный звук)

2. Графический интерфейс:

   - Окно настроек с вкладками

   - Настройка горячих клавиш

   - Инструменты PyAutoGUI

   - Веб-интерфейс (localhost:5050)

3. Автоматизация:

   - Скриншоты автоматически копируются в буфер обмена

   - Автосохранение в папку screenshots/

   - Показ предпросмотра

4. Микросервисная архитектура:

   - Каждый сервис в отдельном процессе

   - IPC коммуникация через Message Bus

   - Возможность горячей замены сервисов

#### Особенности реализации:

1. Модульная архитектура: Каждый сервис в отдельном модуле

2. Горячие клавиши: Настраиваемые комбинации для всех функций

3. Два режима записи звука: Микрофон и системный звук

4. Интуитивный GUI: Современный интерфейс на CustomTkinter

5. Буфер обмена: Автоматическое сохранение результатов

6. Конфигурация: Все настройки сохраняются в JSON файл

#### Архитектура приложения:

```python
OwOCap/
│
├── main.py                             # Главный процесс
├── config.json                         # Конфигурация
├── requirements.txt                    # Зависимости
├── build_exe.py                        # Сборка в исполняемый файл
│
├── gui/                                # Графический интерфейс
│   └── app_gui.py                      # Главное окно
│
├── services/                           # Микросервисы
│   ├── screenshot_service.py           # Захват экрана
│   ├── speech_service.py               # Распознавание речи
│   └── audio_capture_service.py        # Захват звука
│
├── utils/                              # Общие модули
│   ├── hotkey_manager.py               # Менеджер горячих клавиш
│   └── clipboard_manager.py            # Менеджер буфера обмена
│
└── models/                             # Модели
    └── vosk-model-small-ru-0.22/       # Модель распознавания речи
```

#### Предложение по либам (удалить до релиза):

- customtkinter==5.2.2

```python
import customtkinter as ctk
# Создает стильный, современный GUI вместо стандартного tkinter
# https://pypi.org/project/customtkinter/
```

- pillow==12.0.0

```python
from PIL import ImageGrab, Image
# Захват экрана, обработка и конвертация изображений
# надо чекнуть патчноты, там какие-то изменения были,
# которые не поддерживаются в старых версиях при даунгрейде
# https://pypi.org/project/Pillow/
```

- keyboard==0.13.5

```python
import keyboard
# Регистрация и обработка глобальных сочетаний клавиш
# https://pypi.org/project/keyboard/
```

- vosk==0.3.45

```python
from vosk import Model, KaldiRecognizer
# Локальное распознавание русского языка без интернета
# https://pypi.org/project/vosk/
```

- sounddevice==0.5.3

```python
import sounddevice as sd
# Запись звука с микрофона и системного вывода
# https://pypi.org/project/sounddevice/
```

- numpy==2.3.5

```python
import numpy as np
# Работа с аудио-массивами, конвертация форматов
# однажды я начну читать доку, открытую в браузере уже год...
# https://pypi.org/project/numpy/
```

- pycaw==20251023

```python
from pycaw.pycaw import AudioUtilities
# Захват звука из приложений (Windows API для аудио)
#  https://pypi.org/project/pycaw/
```

- comtypes==1.4.13

```python
from comtypes import CLSCTX_ALL
# Работа с Windows COM объектами для аудио
# не думаю что нам нужна поддержка python ниже 3.9,
# поэтому берем просто свежую
# https://pypi.org/project/comtypes/
```

- pyperclip==1.11.0

```python
import pyperclip
# Копирование текста в буфер обмена (проще чем tkinter)
# ❗ НЕ поддерживает изображения на всех платформах (ну тип хз если это минус)
# https://pypi.org/project/pyperclip/
```

- pywin32==311

```python
import win32clipboard
# Копирование изображений в буфер обмена Windows
# Работает ТОЛЬКО на Windows
# https://pypi.org/project/pywin32/
```

- pyinstaller==6.17.0

```python
# Используется только для сборки, не для работы приложения
# python build_exe.py
# этот либ багуется с python 3.10, 3.10 не используем
# https://pypi.org/project/pyinstaller/

```

- screeninfo==0.8.1

```python
from screeninfo import get_monitors
# Определение размеров экрана для скриншотов
# не уверен что либа нужна, если да - лучше поискать замену
# https://pypi.org/project/screeninfo/
```

- mss==10.1.0

```python
import mss
# Более быстрый захват скриншотов, особенно нескольких мониторов
# https://pypi.org/project/mss/
```

# ToDo:

```bash
[✅] поднять репозиторий
[  ] завернуть в desktop app
[  ] настройки приложения:
[  ] - позиция/размер окна ответа
[  ] - тема оформления (л/д)
[  ] - выбор микрофона
[  ] - длительность отображения окна с ответом
[  ] - отображать ли окно с запросом
[  ] автодобавление тематики для промпта
[  ] две ветки. 1 - с нейронкой, 2 - без нейронки, дата в буфер обмена / окно
[  ] режим работы 1: хоткей - скриншот XY - текст с картинки - нейро
[  ] режим работы 3: хоткей - системные звуки приложений - звук в текст - нейро
[  ] режим работы 4: хоткей - звук с микрофона - звук в текст - нейро
[  ] умный режим для 3 и 4: хоткей тогглер - автовыделение тела вопроса из звукового потока
[  ] возможность выбора нейронки для работы по апи (чатгпт, ллама, яндексгпт, мб что-то еще)
[  ] защита от обнаружения
[  ] хоткей для сокрытия интерфейса
[  ] хоткей для моментального изменения окна отображения ответа (размер/позиция)
[  ] хоткей для *активации* окна ответа + возможность копирования
[  ] pytests
[  ] подключить favicon для сборки
[  ] документация
```
